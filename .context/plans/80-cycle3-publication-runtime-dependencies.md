# Plan 80 — Cycle 3 Publication, Runtime, and Dependency Boundaries

**Findings:** C3-008, C3-009, C3-010, C3-011, C3-012, C3-013, C3-014
**Deploy mode:** none
**Status:** completed after the Cycle 18 C3-008 regression repair

## Outcome

Give every published catalog byte set a unique identity, make web and default
CLI analysis consume the same compiled runtime contract, move heavy CSV work
off the UI thread, fail closed on incomplete scraper input, and authenticate or
remove every runtime dependency.

## Tasks

- [x] Build all normalized summary/optimizer/detail/category payloads without
  identity first, compute a deterministic publication ID from the keyed
  serialized payload set (or an automatically derived equivalent contract
  revision), then inject the ID into every artifact and shard.
- [x] Add source-stable/projection-changed and mixed-old/new tests proving that
  changed generated bytes change identity and cannot be combined in one page
  session.
- [x] Add one schema-validating optimizer-artifact reader owned by
  `@cherrypicker/rules`. Use the compiled artifact for default CLI
  optimize/report; retain recursive YAML loading only for an explicit
  `--cards` authoring override and clearly disclose that mode.
- [x] Transfer the original CSV `ArrayBuffer` to the parser worker. Perform
  encoding detection, decoding, replacement counting, bank detection, and CSV
  parsing there, returning warnings/metadata with the result and avoiding
  string structured-cloning.
- [x] Replace scraper source truncation with a completeness-preserving policy.
  This cycle may chunk/merge with validation or reject over-limit source before
  the model call; it must expose original/sent sizes and cannot write an
  ordinary success artifact after omission.
- [x] Declare Zod directly in the scraper workspace and add a dependency
  boundary check for undeclared non-relative production imports.
- [x] Replace the URL-only SheetJS resolution with a content-authenticated,
  reproducible source while preserving the supported API/version behavior:
  an integrity-bearing immutable package, or a vendored immutable archive with
  a checked SHA-256/SHA-512 and a blocking lock-policy gate. Frozen install
  alone is not sufficient.
- [x] Remove the seven unused direct declarations (`@cherrypicker/viz`,
  LayerChart, three direct D3 packages, Papa Parse, and parser `unpdf`) and
  regenerate the lockfile. Add explicit exemptions only for real config/CLI
  consumers.

## Acceptance

- [x] Holding YAML/taxonomy data constant while changing a projection changes
  the publication ID and mixed artifacts fail closed.
- [x] Default CLI and web load the same normalized optimizer graph; a
  fresh-process CLI regression stays within an explicit startup/RSS budget.
- [x] Maximum-size CSV preprocessing does not execute on the browser main
  thread and the worker owns the transferred buffer.
- [x] A 40,001-character scraper source cannot yield an undisclosed successful
  catalog write.
- [x] Every production import is directly declared and every remote dependency
  is content-authenticated.
- [x] Data build/check, dependency policy, parser/web tests, bundle budgets,
  lint, typecheck, and build pass.

## Coverage

| Finding | Completion evidence |
|---|---|
| C3-008 | payload-derived publication identity and mixed-generation tests |
| C3-009 | rules-owned pure parser/Node reader; exact YAML/compiled and web/CLI normalized parity; compiled-default command test; 1,500 ms median-startup and 160 MiB peak-RSS budgets |
| C3-010 | transferable CSV worker payload and main-thread regression |
| C3-011 | explicit over-limit/incomplete scraper result |
| C3-012 | direct Zod declaration and import-boundary gate |
| C3-013 | authenticated SheetJS resolution and lock-policy test |
| C3-014 | removed unused manifests/lock entries |

## Completion evidence

- Publication build/check produced 683 cards, 24 issuer shards, and one
  payload-derived identity across summary, optimizer, details, and categories:
  `392b610e498cc3fc793b41c8cfc572c1f75b50ae853f967433568b829c2cbffd`.
- Projection mutation, mixed-publication, CSV-transfer, scraper-limit,
  dependency-policy, compiled-catalog parity, and fresh-process budget
  regressions passed.
- Three fresh compiled-reader probes remained below the 1,500 ms median
  startup and 160 MiB peak-RSS budgets.
- `vendor/xlsx-0.20.3.tgz` is pinned by both SHA-256
  `8dc73fc3b00203e72d176e85b50938627c7b086e607c682e8d3c22c02bb99fe8`
  and SHA-512
  `a0b0eade3c3b01c2ea2961f60210a9553665f267fa5f661178ff8d7a1d12254cd5fc1759623b61f78b46e6da22301d4f3eb62dc4e09f6a850292fb6e1fedc024`.
- The required repository lint, typecheck, build, Bun test, Vitest, and E2E
  gates passed; dependency policy, data check, and scoped diff checks passed.
- No deployment was performed.

## Cycle 18 reopening — legacy catalog identity

The split browser publication remains correct, but the broader C3-008 outcome
has regressed for supported legacy outputs:

- `scripts/catalog-publication.ts:113-132` hashes browser summary, optimizer,
  detail-shard, and category projections.
- `scripts/build-json.ts:245-329,375-438` builds separate legacy full and
  compact projections, then copies the browser-only hash into both.
- Cycle 17 changed the legacy byte sets, fields, ordering, and compact
  cardinality while version `1.0.0` and source hash
  `ad1edfe624495c7380b86255de27a29091eacc09e325d82a9533034ad2279b58`
  remained unchanged.

This is a confirmed Low / High-confidence regression of C3-008, not a new
Cycle 18 root. Active browser readers remain pinned to the complete split
set, which bounds current impact.

### Reopened tasks

- [x] Extend the publication identity with a canonical keyed supplemental
      payload set.
- [x] Construct identity-free legacy full and compact projections before
      computing or injecting `sourceHash`.
- [x] Include both legacy projections under stable, distinct keys.
- [x] Keep the browser schema at `1.0.0` and mark the changed legacy contract
      as `2.0.0`.
- [x] Add regressions proving a legacy-full-only or compact-only mutation
      changes identity while supplemental key reordering does not.
- [x] Regenerate all affected JSON through `bun run data:build`; never edit
      generated catalogs by hand.
- [x] Prove every browser and legacy artifact carries one complete-publication
      hash and that active readers/bundle budgets remain unchanged.

### Reopened acceptance

- [x] Every supported published byte set contributes to its advertised
      identity.
- [x] No identity input contains an already injected `sourceHash`.
- [x] Legacy full and compact artifacts advertise version `2.0.0`.
- [x] Browser summary, optimizer, categories, all detail shards, and both
      legacy catalogs carry the same new hash.
- [x] Canonical generation, drift checking, focused publication tests, and
      every required repository gate pass.

### Cycle 18 completion evidence

`build-json.ts` now constructs identity-free split, legacy-full, and
legacy-compact projections before calling the canonical publication hasher.
The two legacy payloads enter under stable distinct keys; only afterward is
the resulting identity injected across all outputs. Browser schema version
remains `1.0.0`; all three legacy copies advertise `2.0.0`.

Canonical generation produced one hash across 30 advertised identities
(three legacy copies, summary, optimizer, categories, and 24 detail shards):

```text
125970f582c040a0c6aa728cab49dea1173c1e7fbd96fc297876c5197291c200
```

After removing the expected hash fields and normalizing the intended legacy
version change, all 30 generated artifacts were byte-equivalent to their
previous projections. Focused identity coverage passed 21 tests and 62
expectations, including independent full/compact mutations, supplemental key
reordering, tracked version checks, and common-identity checks.
`data:build`, `data:check`, 30 browser/CLI/rules reader tests, and the web
bundle budget passed. The signed plan-scoped implementation commit is
`9415b1b`.

### Cycle 18 implementation protocol

The requested `ralph` capability is unavailable. Prompt 3 will use the
approved disciplined manual plan-to-test fallback: add focused failing
legacy-mutation regressions, implement the two-phase generator repair, run
the publication suite, run `bun run data:build` and `bun run data:check`,
inspect generated invariants, then run:

- `bun run lint`
- `bun run typecheck`
- `bun run build`
- `bun run test`
- `bun run test:bun`
- `bunx vitest run`
- `bun run test:e2e`

Use the repository E2E ownership preflight and postflight checks. Do not
deploy.
