# Plan 101 — Cycle 6 CI and Documentation Truth

**Findings:** C6-015 (High/High), C6-016 (Medium/High), C6-017 (Medium/High), C6-018 (Medium/High)  
**Status:** complete  
**Deploy mode:** none

## Evidence

- `playwright.config.ts:11-24` enables CI retries without
  `failOnFlakyTests`; `.github/workflows/deploy.yml:44-60` proceeds after a
  retry-only pass.
- `scripts/__tests__/readme-catalog.test.ts` checks README/AGENTS surfaces but
  omits `.claude/CLAUDE.md` and generated regeneration instructions.
- `.claude/CLAUDE.md:7,22` says Astro 6 while
  `apps/web/package.json` requires Astro 7.
- `scripts/build-json.ts:1-5,445-460` and the generated category-label fallback
  prescribe `node --experimental-strip-types scripts/build-json.ts`, which
  fails under the pinned toolchain.

## Outcome

CI treats retry-only browser results as failures, active architecture guidance
derives its Astro major from the manifest, and generated-source instructions
name a command that the repository supports and verifies.

## Implementation

1. Set `failOnFlakyTests: Boolean(process.env.CI)` in the regression Playwright
   config while preserving local retry behavior and trace diagnostics. Extend
   workflow/config consistency tests so the fail-closed contract cannot drift.
2. Update both active architecture-guide references to Astro 7.
3. Make Bun the generator executable contract: use a Bun shebang and one
   canonical regeneration instruction (`bun run data:build`) in source usage,
   generator template, and checked-in generated fallback header.
4. Extend the existing documentation truth suite rather than creating a
   parallel harness. Derive the Astro major from `apps/web/package.json` and
   check all active agent guides; derive the regeneration command from the
   root script contract and check both generator source and generated output.
5. Regenerate/check catalog outputs and ensure only intended generated bytes
   change.

## Tests

- Workflow consistency tests for CI retry plus `failOnFlakyTests`, local
  behavior, artifact diagnostics, and exact regression-config ownership.
- Negative documentation fixtures for a stale Astro major and a stale
  generated regeneration command.
- Execute the documented Bun regeneration command followed by `data:check`.
- Documentation, workflow, data, dependency, lint, type, unit/Vitest, build,
  and final E2E gates.

## Acceptance

- [x] A browser test that passes only on retry fails CI.
- [x] Active architecture guidance cannot claim an Astro major different from
  the web manifest.
- [x] Every emitted regeneration instruction runs in the pinned repository.
- [x] Current documentation/data gates detect both previously false claims.

## Completion evidence

- Regression Playwright configuration now fails retry-only CI results while
  preserving local retries and diagnostics.
- Active architecture guidance identifies Astro 7, and truth tests derive the
  expected major from the web manifest.
- Generator source and generated instructions consistently use
  `bun run data:build`; workflow, documentation, and data checks passed.

## Execution note

`ralph` is unavailable in the installed skills. Prompt 3 will use a manual
test-first loop and will not deploy; only the existing no-deploy branch may be
pushed after every required gate passes.
